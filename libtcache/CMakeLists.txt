FILE(GLOB_RECURSE TCACHE_SOURCES "*.c")
MESSAGE(STATUS "TCACHE_LIB_SOURCES: ${TCACHE_SOURCES}")
add_library(tcache STATIC ${TCACHE_SOURCES})
target_include_directories(tcache PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
